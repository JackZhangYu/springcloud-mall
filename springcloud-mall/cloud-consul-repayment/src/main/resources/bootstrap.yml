
server:
  port: 8206
spring:
  application:
    name: cloud-consul-payment
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: cloud-consul-payment
      config:
        enabled: true
        format: yaml
        prefix: config
        profile-separator: ':'
        data-key: data
